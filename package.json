{
  "name": "sovr-empire-c-and-c",
  "version": "1.0.0",
  "private": true,
  "description": "SOVR EMPIRE COMMAND & CONTROL",
  "scripts": {
    "build": "esbuild index.tsx --bundle --outfile=dist/index.js --jsx=automatic --external:react --external:react-dom/client --external:recharts --external:immer && cp index.html metadata.json dist/",
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@google/genai": "^1.11.0",
    "immer": "^10.1.1"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "typescript": "^5.5.3",
    "vercel": "^35.2.1"
  }
}